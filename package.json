{
  "name": "evaluation-model",
  "version": "1.0.0",
  "description": "OCaml evaluation model",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "format": "prettier *.js src/*.js test/*.js --write",
    "checkstyle": "prettier *.js src/*.js test/*.js --check",
    "test:coveralls": "jest --coverage --coverageReporters=text-lcov | coveralls"
  },
  "keywords": [
    "Eager evaluation",
    "OCaml evaluation model"
  ],
  "author": "mapogolions",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^3.0.5",
    "jest": "^24.8.0",
    "prettier": "1.18.2"
  },
  "dependencies": {}
}
